{
    "buildCommand": "celery -A index.celery worker &",
    "functions": {
        "index.py": {
            "maxDuration": 10
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "outputDirectory": "."
}