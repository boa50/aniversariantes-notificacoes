{
    "buildCommand": "celery -A index.celery worker &",
    "functions": {
        "index.py": {}
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "outputDirectory": "."
}