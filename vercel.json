{
    "buildCommand": "flask --app index run & celery -A index.celery worker &",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ],
    "outputDirectory": "."
}