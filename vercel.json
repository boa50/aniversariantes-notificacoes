{
    "buildCommand": "gunicorn index:app & celery -A index.celery worker &",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./index.py"
        }
    ],
    "outputDirectory": "."
}